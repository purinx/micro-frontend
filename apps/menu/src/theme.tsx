import { extendTheme } from '@chakra-ui/react';

export const menuTheme = extendTheme({
  global: {},
  colors: {
    black: 'rgba(0,0,0,0.6)',
    primary: '#a4f44b',
    secondary: '#a4caf1',
    normal: '#ccc',
  },
});
